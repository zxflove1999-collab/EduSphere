<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.campus.mapper.BookTagMapper">

    <resultMap id="BaseResultMap" type="com.example.campus.entity.BookTag">
        <id column="id" property="id"/>
        <result column="book_id" property="bookId"/>
        <result column="tag_id" property="tagId"/>
    </resultMap>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM t_book_tag WHERE id = #{id}
    </select>

    <select id="selectByBookId" resultMap="BaseResultMap">
        SELECT * FROM t_book_tag WHERE book_id = #{bookId}
    </select>

    <select id="selectByTagId" resultMap="BaseResultMap">
        SELECT * FROM t_book_tag WHERE tag_id = #{tagId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_book_tag (book_id, tag_id)
        VALUES (#{bookId}, #{tagId})
    </insert>

    <delete id="deleteById">
        DELETE FROM t_book_tag WHERE id = #{id}
    </delete>

    <delete id="deleteByBookId">
        DELETE FROM t_book_tag WHERE book_id = #{bookId}
    </delete>

    <delete id="deleteByBookIdAndTagId">
        DELETE FROM t_book_tag WHERE book_id = #{bookId} AND tag_id = #{tagId}
    </delete>

</mapper>

