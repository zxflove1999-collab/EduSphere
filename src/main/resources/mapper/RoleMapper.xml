<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.campus.mapper.RoleMapper">

    <resultMap id="BaseResultMap" type="com.example.campus.entity.Role">
        <id column="role_id" property="roleId"/>
        <result column="role_name" property="roleName"/>
        <result column="role_key" property="roleKey"/>
        <result column="description" property="description"/>
        <result column="is_system" property="isSystem"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM t_role WHERE role_id = #{roleId}
    </select>

    <select id="selectByRoleKey" resultMap="BaseResultMap">
        SELECT * FROM t_role WHERE role_key = #{roleKey}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM t_role
    </select>

    <select id="selectByCondition" resultMap="BaseResultMap">
        SELECT * FROM t_role
        <where>
            <if test="roleName != null and roleName != ''">
                AND role_name LIKE CONCAT('%', #{roleName}, '%')
            </if>
        </where>
        ORDER BY created_at DESC
    </select>

    <select id="selectSimpleList" resultType="map">
        SELECT role_id, role_name FROM t_role ORDER BY role_id
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="roleId">
        INSERT INTO t_role (role_name, role_key, description, is_system)
        VALUES (#{roleName}, #{roleKey}, #{description}, #{isSystem})
    </insert>

    <update id="update">
        UPDATE t_role
        <set>
            <if test="roleName != null">role_name = #{roleName},</if>
            <if test="roleKey != null">role_key = #{roleKey},</if>
            <if test="description != null">description = #{description},</if>
        </set>
        WHERE role_id = #{roleId}
    </update>

    <delete id="deleteById">
        DELETE FROM t_role WHERE role_id = #{roleId}
    </delete>

</mapper>

