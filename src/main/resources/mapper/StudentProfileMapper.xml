<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.campus.mapper.StudentProfileMapper">

    <resultMap id="BaseResultMap" type="com.example.campus.entity.StudentProfile">
        <id column="user_id" property="userId"/>
        <result column="student_id_number" property="studentIdNumber"/>
        <result column="gender" property="gender"/>
        <result column="date_of_birth" property="dateOfBirth"/>
        <result column="major" property="major"/>
        <result column="class_name" property="className"/>
        <result column="grade" property="grade"/>
        <result column="enrollment_date" property="enrollmentDate"/>
        <result column="current_dorm_id" property="currentDormId"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM t_student_profile WHERE user_id = #{userId}
    </select>

    <insert id="insert">
        INSERT INTO t_student_profile (user_id, student_id_number, gender, major, class_name, grade, enrollment_date)
        VALUES (#{userId}, #{studentIdNumber}, #{gender}, #{major}, #{className}, #{grade}, #{enrollmentDate})
    </insert>

    <update id="update">
        UPDATE t_student_profile
        <set>
            <if test="gender != null">gender = #{gender},</if>
            <if test="major != null">major = #{major},</if>
            <if test="className != null">class_name = #{className},</if>
            <if test="grade != null">grade = #{grade},</if>
            <if test="enrollmentDate != null">enrollment_date = #{enrollmentDate},</if>
        </set>
        WHERE user_id = #{userId}
    </update>

    <delete id="deleteByUserId">
        DELETE FROM t_student_profile WHERE user_id = #{userId}
    </delete>

</mapper>

